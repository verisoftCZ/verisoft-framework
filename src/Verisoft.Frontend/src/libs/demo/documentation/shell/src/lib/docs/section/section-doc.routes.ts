import { Routes } from '@angular/router';
import { SectionDocComponent } from './section-doc.component';

export const routes: Routes = [
  {
    path: '',
    component: SectionDocComponent,
  },
];
