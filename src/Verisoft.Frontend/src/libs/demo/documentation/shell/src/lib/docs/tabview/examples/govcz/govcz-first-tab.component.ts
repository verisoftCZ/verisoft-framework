import { Component } from "@angular/core";

@Component({
    selector: 'v-doc-govcz-tabview-first-example',
    standalone: true,
    template: `First tab`,
  })
  export class GovCzTabViewFirstTabExampleComponent {
  }